# AIoT Active Safety Defense Platform - Cursor & Agent Rules

# 語言與溝通規範 (Language & Communication)
* 一律使用 **繁體中文（台灣）** 進行對話、文件撰寫、註解及所有回覆。
* 產出的 `implementation_plan.md`、`task.md`、`walkthrough.md` 等文檔均需使用繁體中文。

# 混合開發與路徑規範 (WSL & Windows Path Hybrid Setup)
* **Windows 工作目錄**：`D:\MyDesktop\antigravity2.0\yolo_db` (所有的程式碼編輯、新增與 Git 版本控制皆在此進行)。
* **Linux WSL 實際部署/執行路徑**：`\\wsl.localhost\Ubuntu\home\edison\aiot_workspace` (在 WSL 中為 `/home/edison/aiot_workspace`)。
* **開發原則**：AI 撰寫路徑、腳本與後端資料庫設定時，必須以 WSL 的環境與路徑為主要部署目標設計，確保軟體能在 Ubuntu WSL 順利執行。
* **系統依賴服務**：WSL 內的 PostgreSQL (`sudo service postgresql start`) 和 Grafana (`sudo service grafana-server start`)。
* **雙向版本一致與啟動驗證**：
  - 每次修改程式後，AI **必須確認使用 Windows 端的 `啟動AIoT專題.bat` 時，系統能透過內建的 `rsync` 機制自動將變更同步至 WSL Ubuntu 中順利進行邊緣運算**。
  - 確認 Windows 目錄與 WSL 執行目錄間沒有任何版本差異，避免舊代碼殘留。
  - **最新代碼執行檢驗 (Critical)**：每次修改檔案後，須指導使用者確認 Node.js 已藉由 `node --watch` 自動熱重載成功；或進行冷啟動前，在 WSL 內使用 `killall node` 清理舊進程，以確保運行的為最新程式碼。

# 低顯存訓練優化規範 (GTX 1650 4GB VRAM)
* 避免 CUDA OOM 錯誤，嚴格配置訓練超參數：
  - `batch=4` 或 `batch=2`
  - `imgsz=416` 或 `imgsz=320`
  - `amp=True` (必須啟用混合精度)
  - `workers=2` 或 `0` (避免 Windows/WSL 記憶體溢出)
  - `cache=False` (禁止快取圖片)
  - 預訓練模型：優先採用輕量化模型 `yolo11n.pt` 或 `yolo11s.pt`。
* 斷點續訓：使用 `resume=True` 連續訓練，如需修改超參數則不使用 `resume=True`，將 `last.pt` 當作一般權重載入再傳入新參數。

# 版本控制與儲存規範 (Git & Storage)
* **自動推送到 GitHub**：凡是進行了代碼變更且驗證無誤後，必須主動執行 Git 提交並上傳（push）到 GitHub。
* 提交倉庫為：`https://github.com/zongyandeng/AIoT.git`，主分支為 `main`。
* **優先使用 D 槽**：新增任何檔案或備份必須優先儲存於 D 槽工作目錄。

# 程式碼品質與撰寫原則
* **拒絕預留位置 (No Placeholders)**：不准使用 `// TODO`、`/* 暫時省略 */` 或 `pass`，必須編寫完整、健壯且可直接執行的程式碼。
* 保持既有文件及註解完整性。
* **主動記錄錯誤與技術學習**：每當遭遇錯誤並解決後，AI 助理**必須主動且系統化地在根目錄的 [TROUBLESHOOTING.md](file:///d:/MyDesktop/antigravity2.0/yolo_db/TROUBLESHOOTING.md) 中新增記錄**，包含**主要問題**、**核心概念**與**解決辦法**，以利使用者持續學習。

*詳細設定與詳細原理解析，請務必參閱專案根目錄下的 [PROJECT_PROMPT.md](file:///d:/MyDesktop/antigravity2.0/yolo_db/PROJECT_PROMPT.md) 檔案。*
